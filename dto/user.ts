export interface UserState {
    first_name: string;
    last_name: string;
    email: string;
    role: string;
    display_pic: string;
    user_id: string;
}

export interface feedback {
    rating: number;
    message: string;
}
